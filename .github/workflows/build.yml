name: Build Maryn Launcher

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          aqtversion: '==3.1.*'

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libfuse2 patchelf

      - name: Build
        run: |
          mkdir build && cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release
          cmake --build . -j4

      - name: Package AppImage
        run: |
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -q https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-qt-x86_64.AppImage
          mkdir -p AppDir/usr/bin AppDir/usr/share/applications AppDir/usr/share/icons/hicolor/256x256/apps
          cp build/Maryn_Launcher AppDir/usr/bin/
          printf '[Desktop Entry]\nName=Maryn Launcher\nExec=Maryn_Launcher\nIcon=maryn-launcher\nType=Application\nCategories=Game;\n' > AppDir/usr/share/applications/maryn-launcher.desktop
          touch AppDir/usr/share/icons/hicolor/256x256/apps/maryn-launcher.png
          QMAKE=$(which qmake6 || which qmake)
          QMAKE=$QMAKE ./linuxdeploy-x86_64.AppImage --appdir AppDir --plugin qt --output appimage

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: Maryn-Launcher-Linux
          path: Maryn_Launcher-*.AppImage

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          arch: 'win64_mingw'
          tools: 'tools_mingw90'
          aqtversion: '==3.1.*'

      - name: Build
        shell: cmd
        run: |
          mkdir build && cd build
          cmake .. -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release
          cmake --build . -j4

      - name: Package
        shell: cmd
        run: |
          mkdir release
          copy build\Maryn_Launcher.exe release\
          windeployqt --no-translations --no-system-d3d-compiler release\Maryn_Launcher.exe

      - name: Zip
        shell: pwsh
        run: Compress-Archive -Path release\* -DestinationPath Maryn-Launcher-Windows.zip

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Maryn-Launcher-Windows
          path: Maryn-Launcher-Windows.zip

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          aqtversion: '==3.1.*'

      - name: Stub missing AGL framework
        run: |
          SDK=$(xcrun --show-sdk-path)
          FWDIR="$SDK/System/Library/Frameworks/AGL.framework"
          if [ ! -d "$FWDIR" ]; then
            sudo mkdir -p "$FWDIR/Headers"
            echo "// stub" | sudo tee "$FWDIR/Headers/agl.h" > /dev/null
            echo '!<arch>' | sudo tee "$FWDIR/AGL" > /dev/null
          fi

      - name: Build
        run: |
          mkdir build && cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0
          cmake --build . -j4

      - name: Sign and strip quarantine
        run: |
          xattr -rd com.apple.quarantine build/Maryn_Launcher.app || true
          codesign --force --deep --sign - build/Maryn_Launcher.app

      - name: Package DMG
        run: |
          cd build
          macdeployqt Maryn_Launcher.app
          xattr -rd com.apple.quarantine Maryn_Launcher.app || true
          codesign --force --deep --sign - Maryn_Launcher.app
          hdiutil create -volname "Maryn Launcher" -srcfolder Maryn_Launcher.app -ov -format UDZO ../Maryn-Launcher-Mac.dmg

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Maryn-Launcher-Mac
          path: Maryn-Launcher-Mac.dmg




  publish-release:
    needs: [build-linux, build-windows, build-mac]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Unzip Windows build
        run: |
          cd artifacts/Maryn-Launcher-Windows
          unzip -o Maryn-Launcher-Windows.zip -d unzipped
          rm Maryn-Launcher-Windows.zip

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest build"
          prerelease: true
          files: |
            artifacts/Maryn-Launcher-Linux/*.AppImage
            artifacts/Maryn-Launcher-Windows/unzipped/**
            artifacts/Maryn-Launcher-Mac/*.dmg
